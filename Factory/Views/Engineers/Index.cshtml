@{
  Layout = "_Layout";
}
@using Factory.Models;
@using System;
@using System.Collections.Generic;
@* @model List<Registrar.Models.Course>;  

<h4>Course List</h4>

@if (@Model.Count == 0)
{
  <h5>Yo, PC Principle. Get some courses!</h5>
} 

<ul>
  @foreach(Course course in Model)
  {
    <li>@Html.ActionLink(course.CourseNumberFull, "Details", new { id = course.DepartmentId }) | Course Name: @course.Name | Student Count: 455</li>
  }
</ul>
<hr/>
<p>@Html.ActionLink("Add new department", "Create")</p> *@